- "bins" contains the relevant PES AI binaries, unzlibbed and ready to be
  modified.
- "data" contains several text files attempting to recreate the .json files
  packed into the AI binaries. The included readme explains those text files
  further.
- "tools" contains currently only two tools that are useful when studying the
  AI binaries and trying to make them a bit more readable.